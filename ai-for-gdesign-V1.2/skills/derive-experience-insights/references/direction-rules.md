# Design direction rules

The number of directions is evidence-driven, not preset. Keep one direction when alternatives add no material value. Create multiple directions only when they express genuinely different experience strategies.

Each direction includes: stable ID, name, status, evidence, core object, entry, organization, journey change, benefits, tradeoffs, conditions, and unanswered questions. `merged` also records `merged_into`.
