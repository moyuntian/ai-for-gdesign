# V1.5 变更与体积

基于桌面 V1.4 创建独立 V1.5，原目录不变。

## 协作与维护

- 四个 Skill 保持独立：入口按任务路由，明确输入、输出、跳过条件；允许自然语言匹配，保留显式调用。
- 安装记录包路径并支持同版本移动后的重新绑定；已知路径及用户授权继续沿用。
- requirements.json、insights.json 与可选 task-handoff.json 统一项目和方向引用；输入哈希和引用检查在生成前执行，简单截图任务无需分析文件。
- 通用、颜色、毛玻璃规范各有维护入口。同步版本策略统一为 keep / patch / minor；默认沿用当前版本。
- 按范围验证、单次同步扫描复用、用途搜索和相关规范路径减少重复工作；完整检查仍用于交付。

## 精简

- 删除预编译 dist、旧版哈希清单、过期验证报告、系统缓存和重复入口。
- 历史设计快照的有效说明并入当前规则；移除过期路径与规则冲突。
- 图标保留 1818 个规范节点、259 个英文别名及原有中文别名；移除 2077 个重复 SVG，使用离线导出脚本按需还原，许可完整保留并进入原型。
- 保留全部 Token、颜色与毛玻璃规范、61 个组件/服务、6 个模板、生成数值表、构建脚本与验证用例。

交付为源码包：使用组件库 npm 导出前运行 build:library。旧 ./icons/* 静态导出改为 scripts/export_icons.py；原型使用 GIcon 的方式保持不变。

## 同版本增量：色块磨砂装饰

- 复用现有毛玻璃专题，新增重点品牌色卡片的选择规则；AI 根据用途、色底和边角留白选用，同组默认仅主卡，不按面积给所有卡片自动装饰。
- 增加 frost-decoration 的 19 个 Token、共用 CSS 绑定与 GMetricCard 变体：品牌蓝弱渐变、统一白字、1–2 个弱白色磨砂图形；原有 Token 和普通卡片默认样式保持不变。
- 接入生成入口、组件规格、资产清单和索引；补充离线 HTML 示例与浏览器回归检查，支持主题、减少透明度和嵌套回退。
- 未另建版本、压缩包或新 Skill；不附带截图、原始素材或构建依赖。

## 文件内容统计

V1.4：2641 个文件，4,710,522 字节（4.71 MB）。

V1.5 初始整理：550 个文件，1,599,941 字节（1.60 MB），减少约 66.0%。

当前 V1.5（含装饰增量）：554 个文件，1,636,640 字节（1.64 MB），较 V1.4 减少约 65.3%。

统计不含测试依赖、构建产物或 ZIP；V1.4 基数包含其原本随附的预编译库及历史文件。磁盘占用会受文件系统分配单元影响。
